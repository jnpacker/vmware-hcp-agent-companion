---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: vmwarenodepooltemplates.vmware.hcp.open-cluster-management.io
spec:
  group: vmware.hcp.open-cluster-management.io
  names:
    kind: VMwareNodePoolTemplate
    listKind: VMwareNodePoolTemplateList
    plural: vmwarenodepooltemplates
    shortNames:
    - vmtemplate
    - vmtpl
    singular: vmwarenodepooltemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.desiredReplicas
      name: Desired
      type: integer
    - jsonPath: .status.currentReplicas
      name: Current
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VMwareNodePoolTemplate is the Schema for the vmwarenodepooltemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VMwareNodePoolTemplateSpec defines the desired state of VMwareNodePoolTemplate
            properties:
              agentISO:
                description: AgentISO defines how to provide the Agent ISO to VMs
                properties:
                  datastorePath:
                    description: |-
                      DatastorePath is the path to the ISO in the vSphere datastore.
                      Required when Type is "datastore".
                      Format: "[datastore-name] path/to/agent.iso"
                    type: string
                  type:
                    description: |-
                      Type specifies how the ISO is provided: "datastore" or "url"
                      - datastore: ISO already exists in vSphere datastore
                      - url: Controller downloads and uploads ISO
                    enum:
                    - datastore
                    - url
                    type: string
                  uploadedISOName:
                    description: |-
                      UploadedISOName is the name to use when uploading the ISO to the datastore.
                      Used when Type is "url". Defaults to "agent-<hash>.iso"
                    type: string
                  url:
                    description: |-
                      URL is the HTTP(S) URL to download the ISO from.
                      Required when Type is "url".
                    type: string
                required:
                - type
                type: object
              agentLabelSelector:
                additionalProperties:
                  type: string
                description: |-
                  AgentLabelSelector defines labels to apply to discovered Agents
                  to associate them with the NodePool
                type: object
              nodePoolRef:
                description: NodePoolRef references the target NodePool to manage
                  VMs for.
                properties:
                  name:
                    description: Name of the NodePool
                    type: string
                required:
                - name
                type: object
              replicas:
                description: |-
                  Replicas defines the number of VMs to create in test mode.
                  Ignored when NodePoolRef is specified (uses NodePool replica count instead).
                format: int32
                type: integer
              testMode:
                description: |-
                  TestMode when true, creates VMs without requiring a NodePool.
                  Useful for validating vSphere connectivity and VM creation.
                type: boolean
              vSphereCredentials:
                description: |-
                  VSphereCredentials references the VMware vSphere credentials.
                  Defaults to looking for a Secret named "vsphere-credentials" in the same namespace.
                properties:
                  name:
                    description: Name of the Secret containing vSphere credentials
                    type: string
                type: object
              vmTemplate:
                description: VMTemplate defines the VMware VM specifications
                properties:
                  advancedConfig:
                    description: AdvancedConfig contains advanced VMware configuration
                      parameters
                    properties:
                      diskEnableUUID:
                        description: |-
                          DiskEnableUUID enables UUID for virtual disks. Required for Kubernetes storage mounting.
                          When enabled, vSphere will assign a UUID to each virtual disk for unique identification.
                          Default: true
                        type: boolean
                      nestedVirtualization:
                        description: |-
                          NestedVirtualization enables hardware-assisted virtualization to the guest OS.
                          This allows the VM to run hypervisors (nested virtualization).
                          Default: true
                        type: boolean
                    type: object
                  cluster:
                    description: Cluster name in vSphere
                    type: string
                  datacenter:
                    description: Datacenter name in vSphere
                    type: string
                  datastore:
                    description: Datastore name for VM storage
                    type: string
                  diskSizeGB:
                    description: 'DiskSizeGB size of the primary disk in GB. Default:
                      120'
                    format: int64
                    type: integer
                  extraConfig:
                    additionalProperties:
                      type: string
                    description: ExtraConfig additional VM configuration options
                    type: object
                  firmware:
                    description: 'Firmware type: bios or efi. Default: "bios"'
                    type: string
                  folder:
                    description: Folder path for VMs. If not specified, VMs are created
                      in the datacenter's VM folder.
                    type: string
                  guestID:
                    description: 'GuestID the guest operating system identifier. Default:
                      "otherLinux64Guest"'
                    type: string
                  memoryMB:
                    description: 'MemoryMB amount of memory in MB. Default: 4096'
                    format: int64
                    type: integer
                  namePrefix:
                    description: |-
                      NamePrefix prefix for VM names. VMs will be named <prefix>-<index>
                      If not specified, uses the template resource name.
                    type: string
                  network:
                    description: Network name to attach VMs to
                    type: string
                  numCPUs:
                    description: 'NumCPUs number of virtual CPUs. Default: 2'
                    format: int32
                    type: integer
                  resourcePool:
                    description: ResourcePool name. If not specified, uses the cluster's
                      root resource pool.
                    type: string
                required:
                - datacenter
                - datastore
                - network
                type: object
            required:
            - agentISO
            - nodePoolRef
            - vmTemplate
            type: object
          status:
            description: VMwareNodePoolTemplateStatus defines the observed state of
              VMwareNodePoolTemplate
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentReplicas:
                description: CurrentReplicas is the current number of VMs
                format: int32
                type: integer
              desiredReplicas:
                description: DesiredReplicas is the desired number of VMs
                format: int32
                type: integer
              isoPath:
                description: ISOPath is the resolved path to the ISO in vSphere
                type: string
              isoUploaded:
                description: ISOUploaded indicates if the ISO has been uploaded (for
                  url type)
                type: boolean
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec.
                format: int64
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of VMs that are ready (powered
                  on)
                format: int32
                type: integer
              vmStatus:
                description: VMStatus contains status information for each VM
                items:
                  description: VMStatus represents the status of a single VM
                  properties:
                    agentName:
                      description: AgentName is the name of the associated Agent resource
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the VM status
                        changed
                      format: date-time
                      type: string
                    message:
                      description: Message provides additional information about the
                        VM status
                      type: string
                    name:
                      description: Name of the VM
                      type: string
                    phase:
                      description: Phase represents the current phase of the VM
                      type: string
                    powerState:
                      description: PowerState of the VM
                      type: string
                    serialNumber:
                      description: SerialNumber of the VM in vSphere for additional
                        agent matching
                      type: string
                    uuid:
                      description: UUID of the VM in vSphere
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
